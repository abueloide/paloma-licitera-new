# REQUIREMENTS.TXT AUDITADO - SOLO DEPENDENCIAS REALMENTE UTILIZADAS

# Core system dependencies
python-dotenv==1.0.0
pyyaml==6.0.1

# Database
psycopg2-binary>=2.9.10
sqlalchemy>=2.0.25

# Data processing
pandas>=2.2.0
python-dateutil==2.8.2

# HTML/Web processing
beautifulsoup4==4.12.2
requests==2.31.0

# PDF Processing - pdfminer como fallback, PyMuPDF se instala dinámicamente
pdfminer.six==20221105

# Web scraping - CRÍTICO para ComprasMX, tianguis, sitios-masivos
playwright>=1.45.0

# API framework - CRÍTICO para src/api.py
fastapi>=0.110.0
pydantic>=2.6.0
uvicorn>=0.27.0

# Scheduler & Automation - Solo si realmente se usa
python-crontab==3.0.0

# AI Processing - CRÍTICO para DOF con IA
anthropic==0.39.0